<UserControl x:Class="View.BookingManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="MinWidth" Value="70" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Booking Management"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20" />

        <!-- Search and Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBox Width="300" Margin="0,0,10,0" Padding="5" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"/>
                <Button Content="Search" Padding="10,5" Command="{Binding SearchCommand}"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Refresh" Padding="10,5" Margin="10,0,0,0"
                    Command="{Binding RefreshCommand}"/>
        </Grid>

        <!-- Bookings DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Bookings}"
                  SelectedItem="{Binding SelectedBooking}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#F5F5F5"
                  BorderThickness="1"
                  BorderBrush="#DDDDDD"
                  VerticalGridLinesBrush="#DDDDDD"
                  HorizontalGridLinesBrush="#DDDDDD"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingId}" Width="90" />
                <DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerId}" Width="90" />
                <DataGridTextColumn Header="Room ID" Binding="{Binding RoomId}" Width="80" />
                <DataGridTextColumn Header="Check-in" Binding="{Binding CheckinTime, StringFormat=d}" Width="110" />
                <DataGridTextColumn Header="Check-out" Binding="{Binding CheckoutTime, StringFormat=d}" Width="110" />
                <DataGridTextColumn Header="Total Price" Binding="{Binding totalPrice, StringFormat=C}" Width="100" />
                <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus}" Width="100" />
                <DataGridTextColumn Header="Booking Date" Binding="{Binding BookingDate, StringFormat=d}" Width="110" />
                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="View Details"
                                        Style="{StaticResource ActionButton}"
                                        Background="#2196F3"
                                        Foreground="White"
                                        Command="{Binding DataContext.ViewDetailsCommand,
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="Cancel Booking"
                                        Style="{StaticResource ActionButton}"
                                        Background="#F44336"
                                        Foreground="White"
                                        Command="{Binding DataContext.CancelBookingCommand,
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Margin="0,15,0,0">
            <TextBlock Text="{Binding StatusMessage}" />
            <Separator />
            <TextBlock Text="{Binding Bookings.Count, StringFormat=Total Bookings: {0}}" />
        </StatusBar>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="4" Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Processing..." Foreground="White" FontSize="18" Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="10"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>